pipeline {
   agent any
     stages {
         stage('verify') {
            steps { 
                    sh " docker --version"
              }
         }
         stage('build') {
          steps {
                    sh "docker-compose build"
              }
          }
          stage('run'){
             step { 
                  sh " docker-compose up"
               }
           }
        }
     }   
